# Domain documentation

This repository uses a single-context layout.

- Product and architectural context lives in `CONTEXT.md`.
- Architecture decision records live in `docs/adr/`.
- Agents should read `CONTEXT.md` before making domain-level changes.
- New durable architectural decisions should be recorded in `docs/adr/`.
